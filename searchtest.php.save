<?php
  include('config.php');
$search_string = $_GET[mac];
   $sql =<<<EOF
       SELECT * from finalproject WHERE MACS LIKE '$search_string' ORDER BY MACS;
   
EOF;

   $ret = $db->query($sql);
   while($row = $ret->fetchArray(SQLITE3_ASSOC) ) {
      echo   $row['IPADDRESS']." | ".$row['VLAN']." | ".$row['PORT']." | "."$_GET[mac]"."\n";
   }
   
   $db->close();
?>

